---

---

<section id="about" class="py-24 bg-white relative overflow-hidden">
  <!-- Background decoration -->
  <div
    class="absolute inset-0 bg-gradient-to-br from-indigo-50/50 via-transparent to-purple-50/50"
  >
  </div>

  <div class="container mx-auto px-4 relative z-10">
    <div class="flex flex-col lg:flex-row items-center gap-16">
      <!-- Image Side -->
      <div class="lg:w-1/2 relative">
        <div class="relative">
          <!-- Decorative elements -->
          <div
            class="absolute -top-8 -left-8 w-72 h-72 bg-gradient-to-br from-indigo-400/20 to-purple-400/20 rounded-full blur-3xl"
          >
          </div>
          <div
            class="absolute -bottom-8 -right-8 w-72 h-72 bg-gradient-to-br from-blue-400/20 to-cyan-400/20 rounded-full blur-3xl"
          >
          </div>

          <!-- Main image -->
          <div class="relative rounded-3xl overflow-hidden shadow-2xl">
            <img
              src="https://images.unsplash.com/photo-1522071820081-009f0129c71c?ixlib=rb-1.2.1&auto=format&fit=crop&w=1000&q=80"
              alt="Dyallab Team"
              class="w-full h-auto"
            />
            <div
              class="absolute inset-0 bg-gradient-to-t from-indigo-900/30 to-transparent"
            >
            </div>
          </div>

          <!-- Floating stat card -->
          <div
            class="absolute -bottom-8 -right-8 bg-white rounded-2xl p-6 shadow-2xl border border-gray-100 animate-float"
          >
            <div class="flex items-center gap-4">
              <div
                class="w-14 h-14 bg-gradient-to-br from-green-400 to-emerald-500 rounded-xl flex items-center justify-center"
              >
                <span class="text-2xl">✓</span>
              </div>
              <div>
                <p class="text-3xl font-bold text-gray-900">99%</p>
                <p class="text-sm text-gray-600">Success Rate</p>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Content Side -->
      <div class="lg:w-1/2 space-y-8">
        <div>
          <div
            class="inline-flex items-center gap-2 bg-indigo-100 text-indigo-700 px-4 py-2 rounded-full font-semibold text-sm mb-6"
          >
            <span class="w-2 h-2 bg-indigo-500 rounded-full animate-pulse"
            ></span>
            ABOUT US
          </div>

          <h2 class="text-5xl md:text-6xl font-extrabold text-gray-900 mb-6">
            Who <span
              class="bg-gradient-to-r from-indigo-600 to-purple-600 bg-clip-text text-transparent"
              >We Are</span
            >
          </h2>

          <div class="space-y-6 text-lg text-gray-600 leading-relaxed">
            <p>
              At Dyallab, we are a team of professionals passionate about
              technology and committed to excellence. Our mission is to help
              companies optimize their technological processes to reach their
              goals.
            </p>
            <p>
              With years of experience in the industry working at the vanguard,
              we have worked with companies of all sizes, from startups to large
              corporations, providing customized solutions that adapt to their
              specific needs at the highest level.
            </p>
          </div>
        </div>

        <!-- Stats Grid -->
        <div class="grid grid-cols-3 gap-6 pt-8">
          <div class="text-center">
            <div class="relative inline-block mb-3">
              <div class="absolute inset-0 bg-indigo-100 rounded-2xl blur-xl">
              </div>
              <div
                class="relative bg-gradient-to-br from-indigo-500 to-purple-600 text-white rounded-2xl p-6 shadow-lg"
              >
                <p class="text-4xl font-bold">20+</p>
              </div>
            </div>
            <p class="text-gray-700 font-medium">Completed Projects</p>
          </div>

          <div class="text-center">
            <div class="relative inline-block mb-3">
              <div class="absolute inset-0 bg-blue-100 rounded-2xl blur-xl">
              </div>
              <div
                class="relative bg-gradient-to-br from-blue-500 to-cyan-600 text-white rounded-2xl p-6 shadow-lg"
              >
                <p class="text-4xl font-bold">5+</p>
              </div>
            </div>
            <p class="text-gray-700 font-medium">Years Experience</p>
          </div>
        </div>

        <!-- CTA Button -->
        <div class="pt-8">
          <a
            href="#contact"
            class="inline-flex items-center gap-2 bg-gradient-to-r from-indigo-600 to-purple-600 text-white px-8 py-4 rounded-xl font-bold hover:from-indigo-700 hover:to-purple-700 transition-all duration-300 shadow-xl hover:shadow-2xl hover:scale-105"
          >
            Work with us
            <svg
              class="w-5 h-5"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
            </svg>
          </a>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  @keyframes float {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-10px);
    }
  }

  .animate-float {
    animation: float 3s ease-in-out infinite;
  }
</style>
